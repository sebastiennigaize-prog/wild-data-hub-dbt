[project]
name = "wild-data-hub-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-bigquery>=1.12.0",
    "pandas<3",
    "pyarrow==22.0.0",
    "pyspark>=4.2.0",
]
